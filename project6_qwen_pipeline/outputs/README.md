# Outputs

Raw Qwen response files live here.

Typical files:

- `category_*.json`: Qwen's category answer, e.g. `{"category":"Chair"}`.
- `layout_*.json`: Qwen's proposed part layout with labeled bounding boxes.

These files should be validated with the CLI before using them downstream.
